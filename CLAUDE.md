# Project notes (read every session)

- Live app URL: https://yinzermcmullet.github.io/macbid-calc/ff/ — **show this link at the end of every reply to the user.**
- End every reply with a clear "Done" marker; keep replies SHORT (user reads on phone, no walls of text).
- Theme: black + gold Pittsburgh (#0b0b0d / #ffb612). NEVER use the SkyeBrite color theme here.
- Fantasy app source: `app/` on branch claude/fantasy-football-dashboard-f54pku. Deploys = build `app/`, copy `app/dist/*` to `ff/` on main (user-approved).
- Root of main = the Mac Bid Calculator site. Don't touch it.
- See PROGRESS.md for roadmap state.
